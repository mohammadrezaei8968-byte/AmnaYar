# اتصال سرویس بانکی امنا یار

نسخه 1.4.0 بک‌اند، استعلام کارت و شبا را از API.ir دریافت می‌کند و در پاسخ نام و نام خانوادگی، بانک، شبا و شماره حساب را در صورت ارائه سرویس برمی‌گرداند. مستندات عمومی API.ir متدهای BankCardInfo، CardToIban و IbanInfo را نشان می‌دهد.

## متغیرهای Render
- `BANK_PROVIDER=api_ir`
- `BANK_API_IR_TOKEN` = توکن واقعی خریداری‌شده از API.ir
- `BANK_API_IR_BASE_URL=https://s.api.ir/api/sw1`
- `BANK_API_IR_IBAN_TO_ACCOUNT_PATH=IbanToAccount` (در صورت تفاوت نام سرویس، دقیقاً مطابق پنل API.ir اصلاح شود)
- `BANK_API_IR_IBAN_TO_CARD_PATH` = فقط اگر سرویس‌دهنده سرویس رسمی شبا→کارت ارائه می‌کند؛ در غیر این صورت خالی بماند.

هیچ رابطه کارت/حساب/شبا در کد حدس زده نمی‌شود. برای شبا→کارت فقط پاسخ رسمی سرویس‌دهنده پذیرفته می‌شود؛ چون یک شبا ممکن است در شرایطی بیش از یک کارت داشته باشد.
